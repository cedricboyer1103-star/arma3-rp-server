// ********************************************************************************************
// Arma 3 RP Server - Configuration File
// ********************************************************************************************

// GLOBAL SETTINGS
hostname = "Arma 3 RP Server";                    // Server name
password = "";                                     // Server password (empty = no password)
passwordAdmin = "adminpass123";                    // Admin password
serverCommandPassword = "commandpass123";          // Server command password for rcon

// SECURITY
maxPlayers = 50;                                   // Maximum number of players
kickDuplicate = 1;                                 // Kick duplicate connections
verifySignatures = 2;                              // Signature verification (2 = force signatures)
equalModRequired = 0;                              // Require same mods as server
allowedFilePatching = 0;                           // File patching (0 = only players with same exe)
requiredBuild = 0;                                 // Minimum build required

// NETWORK SETTINGS
headlessClients[] = {};                            // Headless client IPs
localClient[] = {};                                // Local client IPs
motd[] = {
    "Bienvenue sur notre serveur Arma 3 RP",
    "Respectez les règles du roleplay",
    "Amusez-vous bien!"
};
motdInterval = 5;                                  // Time interval between messages

// VOTING
voteMissionPlayers = 1;                            // Vote for mission when players count
voteThreshold = 0.33;                              // Percentage of votes needed

// MISSION ROTATION
persistent = 1;                                    // Persistent server
autoSelectMission = true;                          // Auto-select mission

class Missions {
    class Mission1 {
        template = "Altis_Life.Altis";           // Mission template
        difficulty = "regular";                   // Difficulty: recruit, regular, veteran
    };
};

// LOGGING
logFile = "logs/server.log";                      // Server log file
timeStampFormat = "short";                        // Timestamp format

// PERFORMANCE
MinBandwidth = 131072;                            // Minimum bandwidth (128 kbps)
MaxBandwidth = 2097152;                           // Maximum bandwidth (2048 kbps)
MaxMsgSend = 512;                                 // Maximum messages per frame
MaxSizeGuaranteed = 512;                          // Maximum packet size
MaxSizeNonguaranteed = 256;                       // Maximum non-guaranteed packet size
MinErrorToSend = 0.001;                           // Minimum error to send
MinErrorToSendNear = 0.01;                        // Minimum error to send near

// DISCONNECT TIMEOUT
disconnectTimeout = 90;                           // Timeout for disconnect (seconds)

// ENABLE/DISABLE BattlEye
BattlEye = 1;                                     // Enable BattlEye anti-cheat
